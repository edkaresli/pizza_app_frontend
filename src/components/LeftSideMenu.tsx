import React from 'react';

import './LeftSideMenu.css';

const LeftSideMenu = (/*props... the menu items, fetched from DB*/) => {
    
    return (
        <div className="left-side-menu">
            <h3>Just a test</h3>
            {/* the menu items presented here */}
        </div>
    );
}

export default LeftSideMenu;